{
  "installCommand": "npm install && npm install --prefix \"FIT HAIR\"",
  "buildCommand": "npm run build",
  "outputDirectory": "FIT HAIR/dist",
  "functions": {
    "api/health.ts": {
      "maxDuration": 10
    },
    "api/pix/charge.ts": {
      "maxDuration": 60
    },
    "api/order/[orderId].ts": {
      "maxDuration": 30
    }
  }
}
